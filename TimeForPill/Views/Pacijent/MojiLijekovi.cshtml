@model IReadOnlyList<MedicineListItemViewModel>
@{
    ViewData["Title"] = "Moji lijekovi";
}

<section class="page-headline">
    <div>
        <p class="eyebrow">Pacijent</p>
        <h1>Moji lijekovi</h1>
    </div>
    <a class="btn btn-primary" asp-action="LijekForma">Dodaj lijek</a>
</section>

@if (!Model.Any())
{
    <section class="empty-state">
        <h2>Jos nema sacuvanih lijekova</h2>
        <p class="muted-text">Dodajte prvi lijek da bi se pojavio u rasporedu i historiji terapije.</p>
        <a class="btn btn-primary" asp-action="LijekForma">Dodaj lijek</a>
    </section>
}
else
{
    <section class="medicine-grid">
        @foreach (var lijek in Model)
        {
            <article class="medicine-card">
                <div class="medicine-image">
                    @if (!string.IsNullOrWhiteSpace(lijek.Slika))
                    {
                        <img src="@lijek.Slika" alt="@lijek.Naziv" />
                    }
                    else
                    {
                        <span>Rx</span>
                    }
                </div>
                <div class="medicine-body">
                    <div class="section-row">
                        <div>
                            <small>@lijek.Kategorija</small>
                            <h2>@lijek.Naziv</h2>
                        </div>
                        <span class="status-badge status-@lijek.Status.ToString().ToLower()">@lijek.Status</span>
                    </div>
                    <dl class="info-list">
                        <div>
                            <dt>Doziranje</dt>
                            <dd>@lijek.DnevnaDoza x dnevno</dd>
                        </div>
                        <div>
                            <dt>Trajanje</dt>
                            <dd>@lijek.Pocetak.ToString("dd.MM.yyyy") - @lijek.Kraj.ToString("dd.MM.yyyy")</dd>
                        </div>
                        <div>
                            <dt>Sljedeca doza</dt>
                            <dd>@lijek.SljedecaDoza</dd>
                        </div>
                    </dl>
                    <a class="btn btn-outline-secondary w-100" asp-action="LijekForma" asp-route-id="@lijek.TerapijaId">Azuriraj lijek</a>
                </div>
            </article>
        }
    </section>
}
